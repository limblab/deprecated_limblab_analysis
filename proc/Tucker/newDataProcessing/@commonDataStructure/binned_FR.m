function FR=binned_FR(cds)
    %this is a method function for the common_data_structure (cds) class, and
    %should be located in a folder '@common_data_structure' with the class
    %definition file and other method files
    %
    %FR=binned_FR(cds) returns a table with the binned firing firing rate of the
    %cds computed using the settings in the bin_settings field of the cds
    for i=1:length(cds.
end